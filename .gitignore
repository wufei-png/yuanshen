# macOS metadata
.DS_Store

# Local QA renders and external visual references
tmp/pdfs/
tmp/references/

# Private real-person source photos; generated assets are tracked instead
/xiting/*.jpg

# Superseded visual drafts kept locally for history, not for the current package
output/nadia_character/assets/archive/
